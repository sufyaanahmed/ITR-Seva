apiVersion: 1

datasources:
  - name: Prometheus
    uid: visa-seva-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 1s
